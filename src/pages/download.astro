---
// 下载页：uvx 一行命令是绝对主角，其余方式一律略写，避免眼花缭乱。
// 命令与主仓 docs/INSTALL.md 保持一致：PyPI 包名 nmail-app、命令名 nmail（必须 uvx --from），勿引入 pipx 等文档外写法。
import Base from '../layouts/Base.astro'
import { REPO_URL } from '../lib/releases'
---

<Base title="下载" description="下载 Nmail：一行命令 uvx 全平台启动，或免安装单文件（Windows · macOS · Linux）。">
  <section class="head">
    <h1>下载 Nmail</h1>
    <p class="sub">推荐下面这一行命令。启动后浏览器自动打开 <code>http://127.0.0.1:8720</code>，下次使用再运行同一条命令即可。</p>
  </section>

  <section class="get">
    <h2>一行命令，全平台启动</h2>
    <div class="onecmd">
      <pre><code>uvx --from nmail-app nmail</code></pre>
      <button class="copy" data-copy="uvx --from nmail-app nmail">复制命令</button>
    </div>
    <p class="hint">没有 uv？官方安装器装一次即可（装完重开终端生效，uv 自动准备 Python 运行时）：</p>
    <div class="inst">
      <span class="os">macOS / Linux</span>
      <pre><code>curl -LsSf https://astral.sh/uv/install.sh | sh</code></pre>
      <button class="copy" data-copy="curl -LsSf https://astral.sh/uv/install.sh | sh">复制</button>
    </div>
    <div class="inst">
      <span class="os">Windows PowerShell</span>
      <pre><code>irm https://astral.sh/uv/install.ps1 | iex</code></pre>
      <button class="copy" data-copy="irm https://astral.sh/uv/install.ps1 | iex">复制</button>
    </div>
    <p class="hint">想常驻安装？<code>uv tool install nmail-app</code>，之后任意目录敲 <code>nmail</code>。</p>
  </section>

  <section class="rest">
    <h2>其他方式</h2>
    <div class="grid">
      <article class="card">
        <h3>📦 免安装单文件</h3>
        <p class="note">不想用命令行？Windows exe / macOS arm64 / Linux x64，下载即用，无需 Python</p>
        <a class="btn btn-primary" href={`${REPO_URL}/releases/latest`} target="_blank" rel="noopener">前往 GitHub Releases ↗</a>
      </article>
      <article class="card">
        <h3>🍎 macOS · Homebrew</h3>
        <p class="note">Apple Silicon；没有 Homebrew？到 <a href="https://brew.sh" target="_blank" rel="noopener">brew.sh</a> 安装</p>
        <pre><code>brew tap nathanpenny520/nmail https://github.com/nathanpenny520/homebrew-nmail
brew install nmail</code></pre>
      </article>
      <article class="card">
        <h3>🪟 Windows · winget（审核中）</h3>
        <p class="note">manifest 审核中，合入前暂不可用——期间用上方单文件或一行命令</p>
      </article>
    </div>
  </section>

  <section class="req">
    <h2>系统要求</h2>
    <ul>
      <li>Windows 10+ / macOS 12+ / 主流 Linux（x64 或 Apple Silicon）</li>
      <li>现代浏览器（Chrome / Edge / Firefox / Safari 最新版）作为界面</li>
      <li>uv 方式零 Python 配置（uv 自动准备运行时）；单文件不需要 Python</li>
      <li>可选：任意 OpenAI 兼容模型 API Key（DeepSeek 等），用于 AI 分类/草稿/总管家</li>
    </ul>
  </section>
</Base>

<style>
  .head { padding: 52px 0 8px; }
  h1 { margin: 0; font-size: 32px; }
  .sub { color: var(--ink-soft); }

  .get { margin: 30px 0 44px; }
  .get h2 { font-size: 24px; margin: 0 0 16px; }
  .onecmd { display: flex; gap: 14px; align-items: stretch; flex-wrap: wrap; }
  .onecmd pre { flex: 1; min-width: 300px; margin: 0; display: flex; align-items: center; }
  .onecmd pre code { font-size: 20px; }
  .copy {
    border: 1px solid var(--line);
    background: #fff;
    border-radius: var(--radius);
    padding: 0 26px;
    font-size: 15px;
    cursor: pointer;
    color: var(--ink);
  }
  .copy:hover { border-color: var(--accent); color: var(--accent); }
  .hint { color: var(--ink-soft); font-size: 13.5px; line-height: 1.8; margin: 14px 0 0; }
  .hint code { font-size: 12.5px; }
  .inst { display: flex; gap: 10px; align-items: stretch; margin: 10px 0 0; flex-wrap: wrap; }
  .inst .os { flex-shrink: 0; align-self: center; font-size: 13px; color: var(--ink-soft); width: 128px; }
  .inst pre { flex: 1; min-width: 260px; margin: 0; display: flex; align-items: center; }
  .inst pre code { font-size: 13px; }
  .inst .copy { border-radius: 8px; padding: 0 14px; font-size: 13px; }

  .rest h2 { font-size: 21px; margin: 0 0 4px; }
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 18px;
    margin: 18px 0 0;
  }
  .card { border: 1px solid var(--line); border-radius: var(--radius); padding: 20px 22px; }
  .card pre { white-space: pre-wrap; word-break: break-all; font-size: 13px; }
  .card h3 { margin: 0 0 4px; font-size: 17px; }
  .note { color: var(--ink-soft); font-size: 13.5px; margin: 4px 0 12px; }
  .card .btn { margin-top: 4px; }

  .req { margin: 44px 0 0; }
  h2 { font-size: 21px; margin: 0 0 8px; }
  ul { color: var(--ink-soft); margin: 8px 0 0; padding-left: 22px; }
  li { margin: 4px 0; }
</style>

<script>
  // 复制按钮：点击把 data-copy 写入剪贴板并短暂反馈
  document.querySelectorAll<HTMLButtonElement>('.copy').forEach((btn) => {
    btn.addEventListener('click', async () => {
      await navigator.clipboard.writeText(btn.dataset.copy ?? '')
      const old = btn.textContent
      btn.textContent = '已复制 ✓'
      setTimeout(() => { btn.textContent = old }, 1500)
    })
  })
</script>
